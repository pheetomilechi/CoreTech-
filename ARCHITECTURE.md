# CoreTech E-Commerce Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         index.html                                  │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ Global Header (Logo, Search, Cart Icon, User Icon)          │   │
│  └──────────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  Page Container (Switches between pages via CSS class)       │   │
│  │  ┌────────────────┬─────────────┬──────────────┬──────────┐  │   │
│  │  │ Home Page      │ Products    │ Product      │ Cart     │  │   │
│  │  │ - Hero         │ - List      │ Detail       │ - Items  │  │   │
│  │  │ - Categories   │ - Filters   │ - Image      │ - Total  │  │   │
│  │  │ - Featured     │ - Sort      │ - Specs      │ - Checkout   │   │
│  │  ├────────────────┴─────────────┴──────────────┴──────────┤  │   │
│  │  │           Checkout Page                                │  │   │
│  │  │           - Shipping Form                              │  │   │
│  │  │           - Payment Method                             │  │   │
│  │  │           - Order Summary                              │  │   │
│  │  └────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
            ┌───────▼────────┐       ┌───────▼────────┐
            │   dist/app.js  │       │   Tailwind     │
            │   Bundled JS   │       │   CSS          │
            └────────┬───────┘       └────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
   ┌────▼─────┐  ┌──────────┐  ┌──▼──────────┐  ┌──────────┐
   │  Router  │  │ State    │  │  UI Handler │  │   App    │
   │          │  │ Manager  │  │             │  │          │
   ├──────────┤  ├──────────┤  ├─────────────┤  ├──────────┤
   │navigate()│  │getCart() │  │render...() │  │init()    │
   │showPage()│  │addCart() │  │showToast() │  │connect() │
   │subscribe│  │products[]│  │filterProd()│  │listen()  │
   └────┬─────┘  └─────┬────┘  └──────┬─────┘  └──────────┘
        │              │              │
        │              │         ┌────▴────────┐
        │              │         │              │
        │         ┌────▴─────┐   │  DOM Updates │
        │         │           │   │  Event      │
        │         │localStorage   │  Listeners  │
        └─────────┴──────────────┘  └────────────┘


FLOW EXAMPLE: User clicks "Add to Cart"

1. User clicks add button
   └─> UIHandler.createProductCard()
   └─> Button click listener triggered

2. stateManager.addToCart(productId)
   └─> Updates state.cart[]
   └─> Saves to localStorage
   └─> Calls notifyListeners()

3. App subscription fires
   └─> uiHandler.updateCartUI()
   └─> Cart count updates
   └─> showToast("Added to cart!")

4. UI Updates without page reload ✨


NAVIGATION FLOW:

User Action → Router.navigate(route) 
   ├─> showPage() [CSS display toggle]
   ├─> notifyListeners(route)
   ├─> App.handleRouteChange()
   └─> UIHandler.render[Page]()


STATE PERSISTENCE:

Product Data (In Memory)
User Actions ──┐
               ├─> StateManager
               ├─> Save to localStorage
               └─> On page reload: Load from localStorage


REACTIVE UPDATES:

stateManager.addToCart()
   └─> setState()
   └─> notifyListeners()
   └─> UIHandler.updateCartUI()
   └─> DOM updates without reload
```

## 🔄 Component Dependencies

```
index.html (HTML structure)
    ├── dist/app.js (Main app logic)
    │   ├── Router (Navigation)
    │   │   └── notifyListeners → App
    │   ├── StateManager (Data)
    │   │   └── notifyListeners → UIHandler
    │   ├── UIHandler (Rendering)
    │   │   ├── uses Router
    │   │   └── uses StateManager
    │   └── App (Orchestration)
    │       └── connects all components
    │
    └── Tailwind CSS (Styling)
```

## 📊 Data Flow Summary

```
┌─────────────────────────────────────────────────┐
│            USER INTERACTION                      │
└────────────────────┬────────────────────────────┘
                     │
                     ▼
        ┌──────────────────────────┐
        │   Event Listener (click) │
        └────────────┬─────────────┘
                     │
                     ▼
        ┌──────────────────────────┐
        │  StateManager.addToCart()│
        │  - Update state.cart[]   │
        │  - Save localStorage     │
        │  - Notify listeners ◄───┐│
        └────────────┬─────────────┘│
                     │              │
                     ▼              │
        ┌──────────────────────────┘
        │
        ▼
    ┌───────────────────────────────┐
    │  App receives notification    │
    │  App.handleRouteChange()      │
    └────────────┬──────────────────┘
                 │
                 ▼
    ┌───────────────────────────────┐
    │  UIHandler.updateCartUI()     │
    │  UIHandler.renderCartPage()   │
    └────────────┬──────────────────┘
                 │
                 ▼
    ┌───────────────────────────────┐
    │  Update DOM (no page reload)  │
    │  - Update cart count badge    │
    │  - Update cart items list     │
    │  - Update total price         │
    │  - Show toast notification    │
    └───────────────────────────────┘
```

This is a **reactive, event-driven** architecture where:
- ✅ Components are loosely coupled
- ✅ State changes trigger UI updates automatically
- ✅ No page reloads needed
- ✅ Smooth page transitions with CSStransformations
- ✅ Persistent cart using browser localStorage
